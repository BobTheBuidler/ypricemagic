commands:
  install: .venv/bin/pip install -e .
  install-dev: .venv/bin/pip install -r requirements-dev.txt
  test: echo "Tests require RPC connection. Run manually per feature description."

services: {}
